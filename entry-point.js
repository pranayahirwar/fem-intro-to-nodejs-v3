#! /usr/bin/env node

/**
 * Entry point for the CLI application.
 * This line is used to call our main app which is inside the src folder.
 * The main app will then call all the modules required for this CLI to work.
 */
import "./src/command.js"